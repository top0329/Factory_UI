import React from 'react';
import SearchBar from '../../components/SearchBar';

const BlueprintPage = () => {
  return (
    <React.Fragment>
      <SearchBar />
    </React.Fragment>
  );
};

export default BlueprintPage;
