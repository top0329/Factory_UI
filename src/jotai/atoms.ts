import { atom } from 'jotai';

export const isAddComponentModalAtom = atom(false);
export const isMintBlueprintModalAtom = atom(false);
export const activeAddComponentTokenAtom = atom(0);

export const searchValueAtom = atom('');
